session_name: ote.dns.reseller
start_directory: ~/ # load session relative to config location (project root).
windows:
- window_name: melbourne
  focus: True
  layout: tiled
  panes:
    - ssh ote-dns1.mel.arsrs.local
- window_name: sydney
  layout: tiled
  panes:
    - ssh ote-dns1.syd.arsrs.local

