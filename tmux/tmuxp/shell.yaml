session_name: shell
start_directory: ~/ # load session relative to config location (project root).
windows:
- window_name: shell
  focus: True
  layout: main-horizontal
  panes:
  - #empty
  - #empty
